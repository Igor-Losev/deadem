{
  "name": "deadem-monorepo",
  "version": "1.5.0",
  "author": "Igor Losev",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present --",
    "proto:json": "npm run proto:json --workspace=deadem",
    "release": "./release.sh",
    "start": "npm run start --workspace=deadem-examples-ui",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^9.27.0",
    "@stylistic/eslint-plugin": "^4.2.0",
    "eslint": "^9.27.0",
    "globals": "^16.2.0",
    "vite": "^6.3.5",
    "vitest": "^3.1.4"
  }
}
