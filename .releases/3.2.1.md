**Bug Fixes**

* `@deademx/engine`, `deadem`: fixed a parse crash on recent Deadlock replays caused by `char` fields without an array count being decoded as null-terminated strings instead of single-byte unsigned integers (`uint8` varints).
