**New Features**

* `@deademx/engine`: Added `SchemaRegistry` APIs for overriding field decoder logic and registering field type decoders, table rules, variable arrays, and encoder overrides.

**Bug Fixes**

* `@deademx/engine`: Added mandatory exports for game packages extending engine bootstrap.

**Improvements**

* `@deademx/engine`: Refactored field decoding internals around registry rules.
* `deadem`, `@deademx/dota2`: Moved game-specific field and string table registrations out of the shared engine bootstrap.
