**New Features**

* `@deademx/ui`: Packets tab — added detail popup for packets with inner messages.
* `@deademx/ui`: Entities tab — added `FieldDefinition` per field, filters, optional alphabetical sort.
* `@deademx/ui`: Entities tab — adjusted filter to accept entitiy handles, made `CHandle` references clickable.
* `@deademx/ui`: Added Tables tab.
* `@deademx/ui`: Diff tab — added String Tables mode.
* `@deademx/ui`: Info tab — String Table Entries widget replaced with the File Header widget.

**Improvements**

* `@deademx/engine`: Improved seek performance by decoding baselines once per class.

**Bug Fixes**

* `@deademx/engine`: `Demo.getEntityByHandle(handle)` now checks the full handle (index + serial) — stale handles return `null`.
* `@deademx/engine`: fixed Dota 2 playback pacing (tick timer ran at 2x speed).
* `deadem`: `HeroID_t` now decodes as unsigned varint.
* `@deademx/ui`: fixed TypedArray value rendering.
* `@deademx/ui`: fixed JSON highlighting (closing brace placement).

**Examples**

* `@deademx/examples-node-dota2`: Added `105_parse_combat_log.js` — combat log.
