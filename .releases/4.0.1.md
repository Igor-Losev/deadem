**Improvements**

* `@deademx/engine`, `deadem`, `@deademx/cs2`, `@deademx/dota2`: Exposed `./package.json` through the `exports`.

**Bug Fixes**

* `ci`: Fixed the proto compilation step.

**Documentation**

* `deadem`, `@deademx/cs2`, `@deademx/dota2`: Corrected the version in the CDN link.

**Examples**

* `@deademx/examples-node-cs2`, `@deademx/examples-node-deadem`: Fixed example scripts for the latest demos (reading `USER_INFO` entries that carry no user data).
